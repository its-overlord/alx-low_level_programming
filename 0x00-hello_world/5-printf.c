#include <stdio.h>

/**
 * main - print to standard output using printf
 * Return: 0 if successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
